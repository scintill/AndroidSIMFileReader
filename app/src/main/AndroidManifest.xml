<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.scintill.simfilereader" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MyActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    
    <permission android:name="net.scintill.simio.RILEXTENDER_CLIENT"
        android:label="@string/perm_RILEXTENDER_CLIENT_label"
        android:description="@string/perm_RILEXTENDER_CLIENT_desc"
        android:permissionGroup="android.permission-group.DEVELOPMENT_TOOLS" />

    <uses-permission android:name="net.scintill.simio.RILEXTENDER_CLIENT" />

</manifest>
